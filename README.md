# Student-Record-Management-System
A Python-based application designed to manage student data. It implements CRUD (Create, Read, Update, Delete) operations and uses file handling to store student information securely in a text file.
